{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open HR Haven website",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run website"
    }
  ]
}
